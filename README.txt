SqlServer 可以不区分表名的大小写

注释: 在sql脚本中 "--" 代表注释掉一行语句

/*

可以注释掉多行语句

*/

使用时去掉注释或者直接复制语句即可使用。

各脚本的功能与名称对应:
createTables.sql 负责创建表格

data_define.sql 负责数据定义 包括基本表的定义，删除，修改，索引的创建，索引的删除。

createView.sql 负责创建视图

testSQL.sql 为测试上述脚本中sql语句的脚本